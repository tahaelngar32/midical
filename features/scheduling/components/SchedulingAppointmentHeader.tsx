import { PageHeader } from "@/components/PageHeader";

const SchedulingAppointmentHeader = () => {
  return (
    <PageHeader
      title="Scheduling Appointments"
      description="Manage your appointments"
    />
  );
};

export default SchedulingAppointmentHeader;
